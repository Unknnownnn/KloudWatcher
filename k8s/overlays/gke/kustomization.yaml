apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: prediction-service-patch.yaml

namespace: kloudwatcher

commonAnnotations:
  gke.io/cluster: "autopilot-cluster-1" 