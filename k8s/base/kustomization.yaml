apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - secrets.yaml

commonLabels:
  app.kubernetes.io/name: kloudwatcher
  app.kubernetes.io/part-of: disaster-management

namespace: kloudwatcher 